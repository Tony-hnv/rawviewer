# RAW View 移动端界面设计

## 设计目标

RAW View 面向需要在安卓设备上快速筛选与整理相机文件的摄影用户。界面采用深灰蓝背景、暖白文字和琥珀色操作强调，避免在户外或暗光环境中造成刺眼感。布局以单手纵向使用为前提：主操作位于拇指易触达的下半屏，关键文件状态以高对比度标签表达。

## 屏幕列表

| 屏幕 | 主要内容 | 核心功能 |
| --- | --- | --- |
| 文件库 | 已导入文件列表、格式筛选、导入入口、最近一次操作提示 | 从系统文档选择器导入 RAW/PNG/JPG/JPEG；按格式筛选；打开详情 |
| 预览详情 | 图片预览或 RAW 文件信息卡、文件名、格式、来源、重命名入口 | 预览可解码的 PNG/JPG/JPEG；为 RAW 尝试显示设备可用的嵌入预览，无法解码时清晰展示文件信息；进入重命名 |
| 重命名面板 | 原文件名、可编辑名称、扩展名锁定提示、确认与取消操作 | 保持扩展名不变地安全重命名；阻止空名称与非法文件名；保存至本地历史 |
| 支持格式说明 | 相机品牌、扩展名、当前可预览能力说明 | 告知 Sony ARW、Canon CR2/CR3、Nikon NEF、Panasonic RW2、PNG、JPG/JPEG 的支持范围 |

## 主要用户流程

用户在文件库点击“导入文件”，调起安卓系统文档选择器并选择一个或多个受支持文件。应用验证扩展名后，将文件元数据保存到本地文件库，并立即显示在最新位置。用户点击某一文件，进入预览详情：PNG/JPG/JPEG 使用设备图像解码能力渲染；RAW 文件优先请求设备可读的图像预览，若设备当前无法解码，则保留文件、显示格式和厂商标记，并明确提示该文件需要启用原生 RAW 解码器才可得到像素级预览。

用户从详情点击“重命名”，系统弹出底部面板。用户只编辑主文件名，扩展名始终锁定为原始格式，以降低误改格式造成文件不可识别的风险。确认后，应用更新本地记录、显示成功反馈，并返回刷新后的详情状态。文件实际位置因 Android Storage Access Framework 的权限与来源差异可能不可直接改写；当系统只授予读取 URI 时，应用会将重命名结果作为本地目录名称保存，并在界面中说明该限制。

## 视觉与布局规范

| 设计元素 | 选择 | 理由 |
| --- | --- | --- |
| 品牌主色 | `#D7983D`（琥珀金） | 体现摄影暗房与暖色高光，用于导入、确认和选中状态 |
| 背景色 | `#11161C`（深石墨蓝） | 减少预览场景中的环境光干扰，强调图像本身 |
| 卡片色 | `#1B242D`（冷灰蓝） | 在深色背景上形成轻量层级，保持专业工具感 |
| 主文字 | `#F4F1EA`（暖白） | 提高暗色环境下的可读性，同时降低纯白刺眼感 |
| 次要文字 | `#AAB4BE`（雾灰） | 用于元数据和说明，不干扰主要操作 |
| 成功状态 | `#55B58A`（青绿） | 表达导入和重命名完成，不与主操作色冲突 |

文件库采用单列紧凑卡片，每张卡片左侧保留 56px 预览区或 RAW 格式占位，右侧按“文件名—格式/厂商—来源”三级信息排列。底部固定保留一个大尺寸“导入文件”操作区，保证单手操作时无需触及顶部。预览详情以图片区域优先，底部信息和重命名操作保持可见；重命名使用系统风格底部面板并避免遮挡输入框。

## 支持范围与降级策略

| 文件类别 | 扩展名 | 应用行为 |
| --- | --- | --- |
| 索尼 RAW | `.arw` | 接受导入并标记为 Sony RAW；优先请求可用预览，未解码时展示 RAW 文件信息 |
| 佳能 RAW | `.cr2`、`.cr3` | 接受导入并标记为 Canon RAW；优先请求可用预览，未解码时展示 RAW 文件信息 |
| 尼康 RAW | `.nef` | 接受导入并标记为 Nikon RAW；优先请求可用预览，未解码时展示 RAW 文件信息 |
| 松下 RAW | `.rw2` | 接受导入并标记为 Panasonic RAW；优先请求可用预览，未解码时展示 RAW 文件信息 |
| 常见图像 | `.png`、`.jpg`、`.jpeg` | 在设备支持时直接渲染预览 |

> 纯 Expo 运行时并不自带面向所有相机 RAW 格式的统一解码器。本版本先完成导入、识别、文件信息预览、可解码预览与安全重命名链路；若要在所有安卓机型上稳定渲染 RAW 像素预览，下一阶段需采用含 LibRaw 等原生解码器的自定义 Android 构建。
